[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
markers =
    db: тест требует настоящей MySQL (пропускается, если её нет)
filterwarnings =
    ignore::DeprecationWarning
