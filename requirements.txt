# Версии зафиксированы по фактически установленному и проверенному набору.
#
# Осторожно с понижением: aiohttp < 3.13 и pydantic < 2.12 не имеют готовых
# сборок под Python 3.14 — pip уходит собирать их из исходников и висит
# десятками минут, а на сервере без компилятора просто падает.
aiogram==3.30.0
sqlalchemy[asyncio]==2.0.51
aiomysql==0.2.0
pymysql==1.1.1
alembic==1.14.0
redis==5.2.1
aiohttp==3.14.3
apscheduler==3.11.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.0.1
openpyxl==3.1.5
# Нужна не нам, а PyMySQL: MySQL 8 по умолчанию использует caching_sha2_password,
# и без cryptography соединение падает на этапе аутентификации.
cryptography==50.0.0
